<?php

return [
    /*
    |--------------------------------------------------------------------------
    | هویت فروشگاه
    |--------------------------------------------------------------------------
    */
    'name'        => env('SHOP_NAME', 'برقی شاپ'),
    'domain'      => env('SHOP_DOMAIN', 'barghishop.com'),
    'phone'       => env('SHOP_PHONE', '021-00000000'),
    'email'       => env('SHOP_EMAIL', 'info@barghishop.com'),
    'address'     => env('SHOP_ADDRESS', ''),

    /*
    |--------------------------------------------------------------------------
    | واحد پول
    |--------------------------------------------------------------------------
    | همه مبالغ در دیتابیس «ریال» و به صورت عدد صحیح ذخیره می‌شوند.
    | display_currency فقط روی نمایش اثر دارد.
    */
    'currency' => [
        'store'   => 'IRR',
        'display' => env('SHOP_DISPLAY_CURRENCY', 'IRT'), // IRT = تومان
        'round_to' => 1000, // گرد کردن مبلغ نهایی به نزدیک‌ترین ۱۰۰۰ ریال
    ],

    /*
    |--------------------------------------------------------------------------
    | مالیات
    |--------------------------------------------------------------------------
    */
    'tax' => [
        'enabled' => env('SHOP_TAX_ENABLED', false),
        'percent' => env('SHOP_TAX_PERCENT', 10),
    ],

    /*
    |--------------------------------------------------------------------------
    | سطوح قیمت
    |--------------------------------------------------------------------------
    */
    'default_tier' => 'retail',

    /*
    |--------------------------------------------------------------------------
    | باشگاه مشتریان
    |--------------------------------------------------------------------------
    */
    'loyalty' => [
        'enabled'            => true,
        'rial_per_point'     => 100_000,  // هر ۱۰٬۰۰۰ تومان خرید = ۱ امتیاز
        'point_value_rial'   => 10_000,   // ارزش هر امتیاز هنگام استفاده
        'award_on_status'    => 'delivered',
        'max_discount_percent' => 20,     // سقف تخفیف امتیازی روی هر سفارش
    ],

    /*
    |--------------------------------------------------------------------------
    | ارسال
    |--------------------------------------------------------------------------
    | حالت‌ها: flat (نرخ ثابت) | weight (استان + وزن) | pickup (حضوری)
    */
    'shipping' => [
        'default_weight_grams' => 1000, // وزن پیش‌فرض محصول بدون وزن
        'free_over'            => env('SHOP_FREE_SHIPPING_OVER', 50_000_000), // ریال
    ],

    /*
    |--------------------------------------------------------------------------
    | درگاه پرداخت
    |--------------------------------------------------------------------------
    */
    'payment' => [
        'default' => env('PAYMENT_DRIVER', 'zarinpal'),
        'gateways' => [
            'zarinpal' => [
                'merchant_id' => env('ZARINPAL_MERCHANT_ID', ''),
                'sandbox'     => env('ZARINPAL_SANDBOX', true),
                'callback'    => '/payment/callback/zarinpal',
            ],
        ],
        'cod_enabled' => env('SHOP_COD_ENABLED', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | پیامک
    |--------------------------------------------------------------------------
    */
    'sms' => [
        'default' => env('SMS_DRIVER', 'limo'),
        'providers' => [
            'limo' => [
                'base_url'  => env('LIMO_SMS_BASE_URL', 'https://api.limosms.com/api'),
                'api_key'   => env('LIMO_SMS_API_KEY', ''),
                'username'  => env('LIMO_SMS_USERNAME', ''),
                'password'  => env('LIMO_SMS_PASSWORD', ''),
                'sender'    => env('LIMO_SMS_SENDER', ''),
            ],
            'log' => [], // درایور توسعه: فقط در sms_logs ثبت می‌کند
        ],
        'patterns' => [
            'otp'            => env('SMS_PATTERN_OTP', ''),
            'order_placed'   => env('SMS_PATTERN_ORDER_PLACED', ''),
            'payment_ok'     => env('SMS_PATTERN_PAYMENT_OK', ''),
            'shipped'        => env('SMS_PATTERN_SHIPPED', ''),
            'wholesale_ok'   => env('SMS_PATTERN_WHOLESALE_OK', ''),
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | ورود با کد یکبار مصرف
    |--------------------------------------------------------------------------
    */
    'otp' => [
        'length'          => 5,
        'ttl_seconds'     => 120,
        'resend_seconds'  => 90,
        'max_attempts'    => 5,
        'max_per_hour'    => 5,
    ],
];
